{
  "version": "1",
  "description": "PM agent GitHub issue reconciliation polling for AutoRAG Research.",
  "tasks": [
    {
      "id": "pm-autorag-issue-poll",
      "name": "Reconcile GitHub Issues Every 15 Minutes",
      "description": "Fallback reconciliation sweep in case GitHub webhooks were missed.",
      "enabled": true,
      "schedule_type": "cron",
      "schedule_value": "*/15 * * * *",
      "context_mode": "group",
      "code_snippet": "const fs = require('fs');\n\nconst repo = (process.env.GITHUB_REPO || '').trim();\nif (!repo) {\n  return { check_error: 'GITHUB_REPO is not set in environment' };\n}\n\nconst token = (process.env.GITHUB_TOKEN || '').trim();\nconst headers = {\n  Accept: 'application/vnd.github+json',\n  'X-GitHub-Api-Version': '2022-11-28',\n  'User-Agent': 'nanoclaw-pm-snippet',\n};\nif (token) headers.Authorization = `Bearer ${token}`;\n\nconst seenPath = '/workspace/group/seen_issues.json';\nif (!fs.existsSync(seenPath)) {\n  fs.writeFileSync(seenPath, JSON.stringify([], null, 2), 'utf8');\n}\n\nlet rawSeen = [];\ntry {\n  rawSeen = JSON.parse(fs.readFileSync(seenPath, 'utf8'));\n} catch {\n  rawSeen = [];\n}\n\nconst seenNumbers = (Array.isArray(rawSeen) ? rawSeen : [])\n  .map((value) => Number(value))\n  .filter((value) => Number.isInteger(value));\nconst seenSet = new Set(seenNumbers);\n\nlet rawIssues;\ntry {\n  const res = await fetch(`https://api.github.com/repos/${repo}/issues?state=open&per_page=100`, {\n    headers,\n  });\n  if (!res.ok) {\n    throw new Error(`HTTP ${res.status} ${res.statusText}`);\n  }\n  rawIssues = await res.json();\n} catch (err) {\n  return {\n    repo,\n    checked_at: context?.run_started_at,\n    reconciliation: true,\n    check_error: `issue fetch failed: ${err instanceof Error ? err.message : String(err)}`,\n    missed_issues: [],\n    all_open_issue_numbers: [],\n    seen_issue_numbers: Array.from(seenSet).sort((a, b) => a - b),\n    seen_file_path: seenPath\n  };\n}\n\nconst issues = [];\nfor (const issue of Array.isArray(rawIssues) ? rawIssues : []) {\n  if (!issue || typeof issue !== 'object') continue;\n  if (issue.pull_request) continue;\n  const number = issue.number;\n  if (!Number.isInteger(number)) continue;\n\n  const labelsRaw = Array.isArray(issue.labels) ? issue.labels : [];\n  const labels = [];\n  for (const label of labelsRaw) {\n    if (typeof label === 'string') labels.push(label);\n    else if (label && typeof label === 'object' && typeof label.name === 'string') labels.push(label.name);\n  }\n\n  issues.push({\n    number,\n    title: typeof issue.title === 'string' ? issue.title : '',\n    body: typeof issue.body === 'string' ? issue.body : '',\n    labels,\n    state: typeof issue.state === 'string' ? issue.state : 'open',\n    url: typeof issue.html_url === 'string' ? issue.html_url : ''\n  });\n}\n\nconst allOpenNumbers = issues.map((issue) => issue.number).sort((a, b) => a - b);\nconst missedIssues = issues.filter((issue) => !seenSet.has(issue.number));\n\nif (missedIssues.length === 0) {\n  return false;\n}\n\nreturn {\n  repo,\n  checked_at: context?.run_started_at,\n  reconciliation: true,\n  missed_issues: missedIssues,\n  all_open_issue_numbers: allOpenNumbers,\n  seen_issue_numbers: Array.from(seenSet).sort((a, b) => a - b),\n  seen_file_path: seenPath\n};",
      "prompt": "Run GitHub issue reconciliation for AutoRAG Research.\n\nInput contract:\n- This task is snippet-gated. Read [SNIPPET_GATE_PAYLOAD] JSON.\n- Payload fields: repo, checked_at, reconciliation, missed_issues, all_open_issue_numbers, seen_issue_numbers, seen_file_path, check_error(optional).\n\n1. Parse [SNIPPET_GATE_PAYLOAD] as JSON.\n2. If payload.check_error exists:\n   - In your final response, output one short Slack alert that includes the exact error and a suggested manual action.\n   - Stop immediately.\n3. Let missedIssues = payload.missed_issues.\n4. If missedIssues is empty, stop immediately.\n5. Treat this as a reconciliation run for webhook misses, not the primary event path.\n6. For each issue in missedIssues:\n   - call gh_issue_linked_prs(repo=\"autorag-research\", issue_number=<number>) and parse the returned JSON\n   - produce triage assessment: scope, risk, missing context, implementation readiness (ready|needs-details)\n   - if any linked PR is OPEN or already merged, treat the issue as already covered and do not implement it again\n   - OPEN and MERGED linked PRs block implementation; CLOSED-unmerged PRs do not block implementation automatically\n   - post gh_issue_comment acknowledging the issue with triage summary, linked PR status, and next step\n   - write_secondbrain_insight with type \"pm-insight\" including issue number/title/body/labels/state/url + triage decision + linked PR status\n   - if readiness is \"ready\" and no linked PR blocks implementation, run this exact implementation workflow:\n     a) git_pull(repo=\"autorag-research\")\n     b) inspect mounted source under /workspace/extra/autorag-research/ and capture concrete file paths, code snippets, observed patterns, dependencies, and constraints\n     c) git_create_branch(repo=\"autorag-research\", branch=\"feature/issue-<number>-<slug>\")\n     d) craft Codex prompt that includes: issue title, issue body, implementation goals, extracted acceptance criteria, reviewed file paths/snippets, project conventions, validation commands, and explicit instructions for Codex to commit, push, and open a PR with `Closes #<number>`\n     e) codex_exec(repo=\"autorag-research\", branch=\"<branch>\", prompt=\"<prompt>\")\n     f) if codex_exec fails or times out, post gh_issue_comment with failure details and \"Tag: human-attention-needed\", then stop this issue workflow\n     g) if codex_exec succeeds, expect the returned output to include PR URL, validation summary, and any follow-up notes; include those in the Slack update\n7. Update /workspace/group/seen_issues.json with payload.all_open_issue_numbers.\n8. Send one short Slack update summarizing reconciled issues, readiness split, blocked-by-linked-pr count, PR links created, validation summary, and human-attention escalations.\n\nTriage comment template:\n\"Thanks for opening this issue. I ran an initial PM triage.\n\n## Initial Assessment\n- Scope: ...\n- Risk: ...\n- Missing context: ...\n- Implementation readiness: ready|needs-details\n- Linked PRs: none | PR #123 (open) ...\n\n## Next Step\n- ...\"\n\nCodex failure comment template:\n\"Implementation automation failed for issue #<number>.\n\n- Failure type: timeout|execution-error\n- Details: ...\n- Next step: human review and manual intervention required.\n- Tag: human-attention-needed\"\n\nSecondBrain template requirements:\n- type: pm-insight\n- source: pm-autorag\n- project: autorag-research\n- title: Issue #<number> triage: <title>\n- tags: [github-issue, triage, reconciliation]\n- links: include issue URL and any linked PR URLs\n- content: issue summary + triage decision + linked PR status + next action",
      "snippet_language": "javascript"
    }
  ]
}
